# Context Snapshot: Embed-Lane Request Parking

## Project shape
Go broker (v2-go branch, worktree ship-embed-parking @ ba4da36): cmd/broker, internal/{admin,config,detect,job,metrics,ollama,plex,proxy,queue,schedule,tdarr,yield}, deploy/broker.service, docs/adr/0001-0008, .claude/skills/broker-* (project law: broker-architecture-contract, broker-change-control).

## CLAUDE.md
none in repo (global user rules apply: commits Preston-only, no Claude attribution)

## CONTEXT.md
Binding vocabulary: Broker / Contention / Yield / Preemption / Job / Queue / Position / Fronting Proxy / Synchronous request. Batch preempted = 503 + caller retries (ADR-0002 — THIS FEATURE SUPERSEDES that for batch-class yield behavior via new ADR-0009).

## ADRs (titles only)
0001 http fronting proxy in Go; 0002 stateless HTTP bounded wait (PARTIALLY SUPERSEDED by this feature's 0009); 0003 hard yield VRAM unload; 0004 GPU scheduling policy; 0005 control token auth; 0006-0008 (see docs/adr/). New ADR here = 0009.

## Key facts
- Code truth: internal/queue/gate.go Yielding() checks short-circuit to 503 before BatchWait — parking hooks there.
- Concurrent session (Plex fixer) has uncommitted WIP in the MAIN checkout (detect/plex/yield-confirm/retry-transport) — shared seam is yield.go/config.go/main.go; rebase before merge, their landed work wins.
- Deploy DEFERRED to drain-end (live LightRAG drain depends on running broker). Merge target = v2-go.
- Durability contract: parked requests fail fast+visibly on shutdown; recovery = LightRAG rescan + LLM-response cache (no re-billing). CPU fallback OUT OF SCOPE.
